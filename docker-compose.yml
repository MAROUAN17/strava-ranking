services:
  ranking-strava:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ranking-strava
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
